
# coding: utf-8

# In[ ]:

from distutils.core import setup
setup(
  name = 'ensemble',
  packages = ['ensemble'], # this must be the same as the name above
  version = '0.1',
  description = 'A package for the ensembling of machine learning models',
  author = 'Prajwal Kailas',
  author_email = 'prajwal967@gmail.com',
  url = 'https://github.com/unnati-xyz/ensemble-package', # use the URL to the github repo
  download_url = 'https://github.com/unnati-xyz/ensemble-package/tarball/0.1', # I'll explain this in a second
  keywords = ['Base Models', 'Ensemble Models', 'Testing'], # arbitrary keywords
  classifiers = [],
)

